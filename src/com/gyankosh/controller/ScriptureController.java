package com.gyankosh.controller;

public class ScriptureController {
    private ScriptureService scriptureService;

    public ScriptureController(ScriptureService scriptureService) {
        this.scriptureService = scriptureService;
    }

    // methods to handle user input and call the appropriate scriptureService methods
}

